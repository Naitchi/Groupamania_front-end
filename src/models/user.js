export default class User {
  constructor(nickname, email, password, phone, birthday) {
    this.nickname = nickname;
    this.email = email;
    this.password = password;
    this.phone = phone;
    this.birthday = birthday;
  }
}
